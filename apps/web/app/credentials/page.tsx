import { OAuthDashboard } from "../components/oauth-dashboard";

export default function CredentialsPage() {
  return (
    <main className="workspace credentials-workspace">
      <OAuthDashboard />
    </main>
  );
}
